{
    "builds": [{
        "src": "video_summary_backend/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.11" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "video_summary_backend/wsgi.py"
        }
    ]
}