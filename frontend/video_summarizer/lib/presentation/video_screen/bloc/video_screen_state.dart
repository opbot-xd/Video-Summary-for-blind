class VideoState{
  bool isLoading;
  VideoState(this.isLoading);
}
class RecordState{
  bool isRecording;
  RecordState(this.isRecording);
}
class TimerState{
  bool timeron;
  TimerState(this.timeron);
}